import pytest

def test_create_folder(client, app)

def test_read_folder(client, app)

def test_update_folder(client, app)

def test_delete_folder(client, app)

def test_existing_folder_name(client, app)